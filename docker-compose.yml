services:
  backend:
    build: ./backend
    container_name: financial_rag_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
      - docfinqa_data:/app/data/docfinqa
      - chroma_data:/app/data/chroma
      - huggingface_cache:/root/.cache/huggingface
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/financial_rag
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4}
      - DOCFINQA_DATA_DIR=/app/data/docfinqa
      - CHROMA_PERSIST_DIR=/app/data/chroma
      - OPTIMIZED_RETRIEVAL_METHOD=${OPTIMIZED_RETRIEVAL_METHOD:-hybrid}
      - OPTIMIZED_RERANKER_ENABLED=${OPTIMIZED_RERANKER_ENABLED:-true}
      - OPTIMIZED_CHUNK_STRATEGY=${OPTIMIZED_CHUNK_STRATEGY:-section}
      - OPTIMIZED_CHUNK_SIZE=${OPTIMIZED_CHUNK_SIZE:-512}
      - OPTIMIZED_TOP_K=${OPTIMIZED_TOP_K:-5}
      - OPTIMIZED_RERANKER_POOL_SIZE=${OPTIMIZED_RERANKER_POOL_SIZE:-20}
    depends_on:
      - db
    
  db:
    image: postgres:16
    container_name: financial_rag_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: financial_rag
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
  chroma_data:
  docfinqa_data:
  huggingface_cache:
